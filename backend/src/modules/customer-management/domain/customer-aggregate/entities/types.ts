export type CreateAddressProps = {
  id: string;
  street: string;
  provinceCode: string;
  provinceName: string;
  wardCode: string;
  wardName: string;
  isDefault: boolean;
};

export type FromPersistentAddressProps = {
  id: string;
  street: string;
  provinceCode: string;
  provinceName: string;
  wardCode: string;
  wardName: string;
  isDefault: boolean;
};
