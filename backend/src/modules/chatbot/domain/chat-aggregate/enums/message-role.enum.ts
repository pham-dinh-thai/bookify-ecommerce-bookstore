export enum MessageRole {
  /** Message from the customer/user. */
  USER = 'user',

  /** Response from the AI assistant. */
  ASSISTANT = 'assistant',

  /** System-level prompt or instruction. */
  SYSTEM = 'system',
}
