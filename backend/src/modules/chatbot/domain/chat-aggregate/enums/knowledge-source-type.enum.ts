export enum KnowledgeSourceType {
  /** Frequently asked questions. */
  FAQ = 'faq',

  /** Store policies (shipping, returns, payment, etc.). */
  POLICY = 'policy',

  /** Product-related content (book descriptions, categories). */
  PRODUCT = 'product',

  /** Custom or manually added content. */
  CUSTOM = 'custom',
}
